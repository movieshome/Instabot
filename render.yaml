version: "1"
services:
- type: web
  name: Instabot
  runtime: python
  pythonVersion: 3.11.7
  plan: free
  region: virginia
  buildCommand: pip install -r requirements.txt
  startCommand: python bot.py
  envVars:
  - key: BOT_TOKEN
    sync: false
